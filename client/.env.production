# Production environment configuration
# Empty VITE_API_URL enables automatic Same-Origin relative routing on localhost, LAN IPs, and web servers.
# Native mobile apps (Android APK) automatically fall back to the cloud instance if not configured.
VITE_API_URL=
